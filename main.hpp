#ifndef __MAIN_HPP__
#define __MAIN_HPP__

#include <fstream>
#include <map>
#include <string>
#include <vector>

// Streams for logging (defined in main.cpp)
extern std::ofstream log;
extern std::ofstream compactLog;

#endif // __MAIN_HPP__
